# 🚀 دليل المساهمة في المشروع

## مرحباً بك! 👋

شكراً لاهتمامك بالمساهمة في نظام إدارة المحامين v7.0. نرحب بجميع المساهمات سواء كانت:
- 🐛 إصلاح الأخطاء
- ✨ ميزات جديدة
- 📝 تحسين التوثيق
- 🎨 تحسينات التصميم
- ⚡ تحسينات الأداء

## 📋 قبل البدء

### المتطلبات الأساسية

- Node.js 14.0+ و npm 6.0+
- Firebase CLI
- محرر نصوص (VS Code موصى به)
- Git

### إعداد بيئة التطوير

1. **استنساخ المشروع**
```bash
git clone https://github.com/yourusername/lawyer-management-system.git
cd lawyer-management-system
```

2. **تثبيت الأدوات**
```bash
npm install -g firebase-tools
```

3. **تشغيل الخادم المحلي**
```bash
firebase serve
# أو
python -m http.server 8000
```

## 🔄 عملية المساهمة

### 1. Fork المشروع

اضغط على زر "Fork" في أعلى الصفحة

### 2. إنشاء فرع جديد

```bash
git checkout -b feature/amazing-feature
```

أنواع الفروع:
- `feature/` - ميزات جديدة
- `fix/` - إصلاح أخطاء
- `docs/` - تحديثات توثيق
- `style/` - تحسينات تصميم
- `refactor/` - إعادة هيكلة الكود
- `test/` - اختبارات
- `perf/` - تحسينات أداء

### 3. قم بالتعديلات

اتبع معايير الكود الموضحة أدناه

### 4. Commit التغييرات

```bash
git add .
git commit -m "نوع: وصف مختصر للتغيير"
```

أمثلة على رسائل Commit الجيدة:
- `feat: إضافة نظام البحث المتقدم`
- `fix: إصلاح مشكلة عرض التاريخ`
- `docs: تحديث دليل الاستخدام`
- `style: تحسين تصميم الأزرار`
- `perf: تحسين أداء تحميل البيانات`

### 5. Push للمستودع الخاص بك

```bash
git push origin feature/amazing-feature
```

### 6. إنشاء Pull Request

- افتح Pull Request من فرعك إلى `main`
- اكتب وصفاً واضحاً للتغييرات
- أضف screenshots إذا كانت التغييرات بصرية
- اربط Issues ذات الصلة

## 📝 معايير الكود

### HTML

```html
<!-- ✅ جيد -->
<div class="card" id="client-card">
  <h3 class="card-title">عنوان البطاقة</h3>
  <p class="card-text">محتوى البطاقة</p>
</div>

<!-- ❌ سيء -->
<div class=card id=clientCard>
  <h3>عنوان البطاقة</h3>
</div>
```

**القواعد:**
- استخدم دائماً علامات اقتباس مزدوجة
- استخدم kebab-case للأسماء
- استخدم علامات دلالية مناسبة
- تأكد من إمكانية الوصول (a11y)

### CSS

```css
/* ✅ جيد */
.card {
  padding: 1rem;
  margin-bottom: 1rem;
  border-radius: var(--radius);
  background: var(--card-bg);
  box-shadow: var(--shadow);
}

/* ❌ سيء */
.card{padding:1rem;margin-bottom:1rem;}
```

**القواعد:**
- استخدم CSS Custom Properties
- مسافة بعد النقطتين وقبل الأقواس
- سطر واحد لكل خاصية
- ترتيب منطقي للخصائص:
  1. Positioning
  2. Box Model
  3. Typography
  4. Visual
  5. Misc

### JavaScript

```javascript
// ✅ جيد
function addLawyer(lawyerData) {
  if (!lawyerData.name) {
    showNotification('الرجاء إدخال الاسم', 'error');
    return;
  }
  
  const lawyer = {
    id: Date.now(),
    ...lawyerData,
    createdAt: new Date().toISOString()
  };
  
  saveLawyer(lawyer);
  showNotification('تم إضافة المحامي بنجاح', 'success');
}

// ❌ سيء
function addLawyer(d){
  let l={id:Date.now(),...d}
  saveLawyer(l)
}
```

**القواعد:**
- استخدم camelCase للمتغيرات والوظائف
- استخدم const/let بدلاً من var
- تعليقات واضحة بالعربية
- معالجة الأخطاء دائماً
- تحقق من المدخلات
- أسماء واضحة ومعبرة

## 🎨 إرشادات التصميم

### الألوان

استخدم CSS Custom Properties المعرّفة:
```css
--primary: #667eea
--primary-dark: #5568d3
--success: #48bb78
--danger: #f56565
--warning: #ed8936
```

### التباعد

استخدم مضاعفات 0.25rem:
```css
padding: 0.5rem;  /* 8px */
padding: 1rem;    /* 16px */
padding: 1.5rem;  /* 24px */
padding: 2rem;    /* 32px */
```

### الرسوم المتحركة

استخدم الانتقالات المعرّفة:
```css
transition: var(--transition-fast);   /* 0.15s */
transition: var(--transition-normal); /* 0.3s */
transition: var(--transition-slow);   /* 0.5s */
```

## 🧪 الاختبار

قبل إرسال Pull Request:

### اختبار يدوي

- [ ] اختبر على Chrome/Edge
- [ ] اختبر على Firefox
- [ ] اختبر على Safari (إن أمكن)
- [ ] اختبر على الهاتف المحمول
- [ ] اختبر على التابلت
- [ ] تحقق من الـ Console للأخطاء

### اختبار التصميم المتجاوب

- [ ] 320px (هاتف صغير)
- [ ] 375px (هاتف متوسط)
- [ ] 768px (تابلت)
- [ ] 1024px (لابتوب)
- [ ] 1440px+ (شاشة كبيرة)

### اختبار الوظائف

- [ ] تسجيل الدخول
- [ ] إضافة/تعديل/حذف البيانات
- [ ] البحث والفلترة
- [ ] التصدير
- [ ] التقارير

## 📦 بنية المشروع

```
project/
├── public/              # الملفات العامة
│   ├── index.html      # الصفحة الرئيسية
│   ├── app.js          # منطق التطبيق
│   ├── style.css       # التنسيقات
│   ├── manifest.json   # PWA manifest
│   ├── sw.js           # Service Worker
│   └── 404.html        # صفحة الخطأ
├── backend/            # Backend (اختياري)
├── firebase.json       # إعدادات Firebase
├── package.json        # Dependencies
├── README.md           # التوثيق الرئيسي
├── CHANGELOG.md        # سجل التغييرات
└── DEPLOYMENT_GUIDE.md # دليل النشر
```

## 🔍 مراجعة الكود

سيتم مراجعة Pull Requests بناءً على:

- ✅ جودة الكود
- ✅ الالتزام بالمعايير
- ✅ التوثيق الكافي
- ✅ عدم كسر الوظائف الحالية
- ✅ الاختبار الشامل
- ✅ تحسين الأداء

## 📞 الحصول على المساعدة

إذا كان لديك أسئلة:

- 📧 أنشئ Issue جديد
- 💬 راجع Issues الموجودة
- 📖 راجع التوثيق

## 🏆 المساهمون

شكراً لجميع المساهمين الرائعين! 🎉

## 📄 الترخيص

بالمساهمة في هذا المشروع، توافق على أن مساهماتك ستكون مرخصة بموجب ترخيص ISC.

---

**شكراً لك على المساهمة! 🙏**