# منصة حسابات المحامين | Lawyer Accounting Platform

منصة شاملة لإدارة حسابات مكاتب المحاماة مع نظام محاسبة متكامل وإدارة القضايا والموكلين.

## 🚀 المميزات

### الواجهة الأمامية (Frontend)
- ✨ واجهة عربية حديثة وسهلة الاستخدام
- 📊 لوحة تحكم شاملة مع إحصائيات تفاعلية
- 👥 إدارة الموكلين والمحامين
- ⚖️ إدارة القضايا والملفات
- 💰 نظام محاسبة كامل (دائن/مدين)
- � **نظام الديون والدائنين المتقدم** (جديد!) - إدارة شاملة لـ 6 أنواع ديون
- �📈 تقارير مالية وإحصائيات
- 🔐 نظام صلاحيات متقدم (Admin/Lawyer/Accountant)
- 💾 دعم العمل دون اتصال (localStorage fallback)
- 🎨 تصميم متجاوب لجميع الشاشات

### الواجهة الخلفية (Backend)
- 🔧 ASP.NET Core 8 Web API
- 🗄️ Entity Framework Core + SQLite
- 🔐 JWT Authentication
- 🔒 BCrypt Password Hashing
- 📝 Activity Logging System
- 🎯 Role-Based Access Control (RBAC)
- 📊 RESTful API Design
- 🔄 CORS Enabled
- 📖 Swagger/OpenAPI Documentation

## 🛠️ التقنيات المستخدمة

### Frontend
- HTML5, CSS3, JavaScript (Vanilla)
- Google Fonts (Cairo)
- Responsive Design
- Local Storage API

### Backend
- .NET 8/10
- ASP.NET Core Web API
- Entity Framework Core 8.0.7
- SQLite Database
- JWT Bearer Authentication
- BCrypt.Net-Next 4.0.3
- Swashbuckle (Swagger) 6.6.2

## 📦 التثبيت والتشغيل

### المتطلبات
- .NET 8 SDK أو أحدث (يدعم .NET 10 مع RollForward)
- متصفح حديث (Chrome, Firefox, Edge)

### تشغيل Backend

```powershell
# الانتقال إلى مجلد Backend
Set-Location backend

# تثبيت المكتبات
dotnet restore

# تشغيل الخادم على المنفذ 5088
dotnet run --urls "http://localhost:5088"
```

البيانات الافتراضية:
- **اسم المستخدم:** admin
- **كلمة المرور:** admin123

### تشغيل Frontend

ببساطة افتح ملف `index.html` في المتصفح أو:

```powershell
Start-Process index.html
```

## 🔗 API Endpoints

### Authentication
- `POST /api/auth/login` - تسجيل الدخول

### Users
- `GET /api/users` - قائمة المستخدمين
- `POST /api/users` - إضافة مستخدم
- `PUT /api/users/{id}` - تحديث مستخدم
- `DELETE /api/users/{id}` - حذف مستخدم

### Clients
- `GET /api/clients` - قائمة الموكلين
- `GET /api/clients/{id}` - تفاصيل موكل
- `POST /api/clients` - إضافة موكل
- `PUT /api/clients/{id}` - تحديث موكل
- `DELETE /api/clients/{id}` - حذف موكل

### Lawyers
- `GET /api/lawyers` - قائمة المحامين
- `GET /api/lawyers/{id}` - تفاصيل محامي
- `POST /api/lawyers` - إضافة محامي
- `PUT /api/lawyers/{id}` - تحديث محامي
- `DELETE /api/lawyers/{id}` - حذف محامي

### Cases
- `GET /api/cases` - قائمة القضايا
- `GET /api/cases/{id}` - تفاصيل قضية
- `POST /api/cases` - إضافة قضية
- `PUT /api/cases/{id}` - تحديث قضية
- `DELETE /api/cases/{id}` - حذف قضية

### Transactions
- `GET /api/transactions` - قائمة العمليات المالية
- `POST /api/transactions` - إضافة عملية
- `PUT /api/transactions/{id}` - تحديث عملية
- `DELETE /api/transactions/{id}` - حذف عملية

### Reports
- `GET /api/reports/summary` - ملخص إحصائي
- `GET /api/reports/client-balances` - أرصدة الموكلين
- `GET /api/reports/monthly-revenue?year={year}` - الإيرادات الشهرية

### Settings
- `GET /api/settings/company` - معلومات الشركة
- `POST /api/settings/company` - تحديث معلومات الشركة

## 👥 الأدوار والصلاحيات

### المدير (Admin)
- كامل الصلاحيات
- إدارة المستخدمين
- إدارة الصلاحيات المخصصة
- عرض سجل النشاطات

### المحامي (Lawyer)
- إدارة القضايا
- عرض الموكلين
- عرض التقارير الأساسية

### المحاسب (Accountant)
- إدارة العمليات المالية
- عرض التقارير المالية
- إدارة الموكلين والمحامين

## 📝 ملاحظات

- **قاعدة البيانات:** يتم إنشاء ملف `lawyer_platform.db` تلقائياً عند أول تشغيل
- **البيانات الأولية:** يتم إدخال بيانات تجريبية تلقائياً (موكلين، محامين، قضايا، عمليات)
- **النسخ الاحتياطي:** يوجد نسخة احتياطية كاملة في ملف ZIP
- **الوضع المزدوج:** يعمل Frontend مع Backend API أو localStorage عند عدم توفر الاتصال

## 🔒 الأمان

- تشفير كلمات المرور باستخدام BCrypt
- JWT Tokens مع صلاحية 8 ساعات
- CORS محدد للنطاق المحلي
- سجل نشاطات شامل لجميع العمليات

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 🤝 المساهمة

نرحب بجميع المساهمات! يمكنك:
- فتح Issue للإبلاغ عن مشكلة
- إرسال Pull Request لتحسين
- اقتراح ميزات جديدة

## 📧 التواصل

لأي استفسارات أو دعم فني، يرجى فتح Issue في المستودع.

---

**الإصدار:** v6.0 Backend Integrated  
**تاريخ آخر تحديث:** نوفمبر 2025
